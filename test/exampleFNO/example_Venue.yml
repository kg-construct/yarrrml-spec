mappings:
 TriplesMap_0:
  sources:
   access: src/test/resources/exampleFNO/Venue.json~jsonpath
   iterator: $
  subjects: "http://loc.example.com/city/$(city})"
  classes: schema+City
  predicateobjects:
   - predicates: ex+containsS
     fn: grel+contains
     parameters:
      - {p: grel+subStringParameter, o: s}
      - {p: grel+valueParameter, o: $(city)}
