sources:
 person_source: "src/test/resources/exampleFn/input.csv"

mappings:
 Person_TemplateMapping:
  sources: person_source
 IntermediateCoordinatesGeoMapping:
  sources: person_source
  predicateobjects:
   - [geo+lon,dbf+lon~fn] #maybe don't need to add '~fn' if there are parameters
      parameters:
       "dbf:lonParameter":
        constant: $(resting_place_coordinates)